export enum Routes {
  HOME = "/",
  CONTACT = "/contact",
  RESUME = "/resume",
  BLOG = "/blog",

  WEB_DEVELOPMENT = "/web-development",
  GAME_DEVELOPMENT = "/game-development",
  DEVOPS = "/devops",
}
